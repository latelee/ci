git:
  quiet: true

sudo: required

services:
  - docker

#language: c

install:
  - apt get install sshpass
  
install:
  - echo "install sth here"
before_script:
  #- chmod +x ./build.sh
script:
  - echo "aaaabc"
  - pwd
  - gcc -v
  - ls -l
  - docker build -t k8s_tar 1.8.15
  - docker images
  #- ./build.sh

after_script:
  - echo "after script"
  - echo "env " $PASS
env:
  global:
    - PASS: latelee
