git:
  quiet: true

language: c

install:
  - apt get install sshpass
  
install:
  - echo "install sth here"
before_script:
  #- chmod +x ./build.sh
script:
  - echo "aaaab"
  - pwd
  - gcc -v
  - ls -l
  - ./build.sh

after_script:
  - echo "after script"
  - echo "env " $PASS
env:
  global:
    - PASS: latelee
