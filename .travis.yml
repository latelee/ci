git:
  quiet: true

# ÷∏∂®”Ô—‘
language: c

install:
  echo "install sth here"
before_script:
  echo "before script"
script:
  - echo "aaaa"
  - pwd
  - gcc -v
  - ls -lh
  - chmod +x build.sh
  - ./build.sh

after_script:
  echo "after script"

